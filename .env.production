# Production env for the client build (safe to commit — publishable key + URLs only).
# NOTE: Vercel dashboard env vars OVERRIDE this file at build time. This file is
# the source of truth / fallback; the Vercel project must have the same values.
# NEVER put the Moyasar SECRET key (sk_live_...) here — it lives only on the server.

# Moyasar (live)
VITE_MOYASAR_PUBLISHABLE_KEY=pk_live_mT9CBgsMYtqMWj2bwZBWntsCtqWAddmdj7cSKQ6k
VITE_MOYASAR_API_BASE_URL=https://server-link-190979667993.europe-west3.run.app
VITE_MOYASAR_CALLBACK_URL=https://www.link-22.com/client/payment-callback
